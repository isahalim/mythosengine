# The only profile that has to work (ARCHITECTURE.md §3). No secrets here —
# only driver selection. GROQ_API_KEY etc. come from the environment.
LLM_DRIVER=groq
ASR_DRIVER=yt-captions
EMBED_DRIVER=local-minilm
VECTOR_DRIVER=sqlite-vec
CACHE_DRIVER=kv
EXPORT_DRIVER=kv-blob
